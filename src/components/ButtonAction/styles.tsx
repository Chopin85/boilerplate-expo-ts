import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  button: {
    alignSelf: "center",
    paddingVertical: 8,
    backgroundColor: "red",
    width: "70%",
    textAlign: "center",
    alignItems: "center",
    marginBottom: 8,
  },
});

export default styles;
