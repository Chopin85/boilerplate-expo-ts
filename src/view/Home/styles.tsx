import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  header: {
    fontSize: 20,
  },
  image: {
    width: (20 * 4) / 3,
    height: (16 * 5) / 6,
    alignSelf: "center",
  },
});

export default styles;
